// API Configuration
export const API_CONFIG = {
  BASE_URL: "http://localhost:8000", // Update this to your FastAPI server URL
  ENDPOINTS: {
    CHAT: "/chat", // Adjust endpoint path as needed
  },
};
